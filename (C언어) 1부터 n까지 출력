#include <stdio.h>

void main()
{
    int n;
    int i = 1;
    printf("숫자를 입력하세요 : ");
    scanf("%d", &n);
    
    if (n <= 0) {
        printf("0보다 큰 자연수를 입력하세요.");
    } else {
        while (i <= n) {
            i++;
            printf("%d ", (i - 1));
        }    
    }
}
