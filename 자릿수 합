#include <stdio.h>

int main()
{
    int k = 0;
    int sum = 0;
    
    scanf("%d", &k);
    
    for (int i = k; i != 0; i = i / 10)
    {
        sum = sum + (i % 10);
    }
    printf("%d", sum);
    
    return 0;
}    
